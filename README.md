# misc-scripts
1. **findPS4s.py** - *Script that searches for active PS4(s) on the local network*
2. **asciiChart.asm** - *"Hello World" MASM script that prints an ASCII table*
3. **getComputerName.asm** - *"Hello World" MASM script that gets the computer name*
4. **getSeparator.asm** - *"Hello World" MASM script that gets the current field separator*
5. **xor.asm** - *"Hello World" MASM script that implements a simple xor encryption*

